<service
    android:name=".LockForegroundService"
    android:exported="false"
    android:foregroundServiceType="specialUse">
    <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="Blocks distracting apps until the user completes an exercise or prayer-rug check, at the user's own request" />
</service>

<activity
    android:name=".LockActivity"
    android:exported="false"
    android:theme="@style/LaunchTheme"
    android:launchMode="singleTask"
    android:showWhenLocked="false"
    android:turnScreenOn="false" />

<receiver android:name=".AthanLockReceiver" android:exported="false" />

<receiver android:name=".BootReceiver" android:exported="false">
    <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
    </intent-filter>
</receiver>

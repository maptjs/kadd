# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
*.g.dart

# Signing secrets — never commit these, regardless of where they end up
*.jks
*.keystore
key.properties

# Android (generated by `flutter create`, not committed — CI regenerates it)
android/

# iOS (not targeted by this project, but ignore if `flutter create` adds it)
ios/

# IDE
.idea/
.vscode/
*.iml

# macOS
.DS_Store
